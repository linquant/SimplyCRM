{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <div id="welcome">
                <h1> Liste des clients</h1>


            <a href=" {{ path('addCustomer') }} ">Ajouter Client</a>


                <table>

                        {% for customer in customer_liste  %}
                    <tr>

                        <th>{{ customer.nom }}</th>
                        <th>{{ customer.prenom }}</th>
                        <th>{{ customer.societe }}</th>
                        <th><a href=" {{ path('detailCustomer',{'id':  customer.id }) }} ">Détails</a></th>

                    </tr>

                        {% endfor %}

                </table>
                <br>
                <p>Index de pagination</p>

                {% for i in 1..NbreDePage %}
                    <a href=" {{ path('Customer',{'page':  i }) }} "> {{ i }}</a>
                {% endfor %}
            </div>



        </div>
    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
